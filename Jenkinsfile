pipeline {
    agent any

    stages {
        stage('test') {
            steps {
                echo 'Hello World'
                sh "ls"
                sh "cfg.sh"
            }
        }
    }
}
